server {
  port = 50049
}

client {
  ip = "192.168.1.154"
  port = 50001
}

oss {
  endpoint = "oss-cn-beijing.aliyuncs.com"
  accessKeyId = "LTAIOMx9OjsM3JJU"
  accessKeySecret = "xVzgTX3nkSsDX61p7KpyjdANz8RjD2"
  bucketName = "logi-dev"
}

redis {
  maxTota = 10000
  maxIdle = 2000
  maxWaitMillis = 100
  testOnBorrow = true
  ip = "127.0.0.1"
  port = 6379
  timeout = 100000
}

mysql {
  username = "root"
  password = " "
  url = "jdbc:mysql://localhost:3306/logivisual?characterEncoding=utf8&useSSL=false"
  diverClass = "com.mysql.jdbc.Driver"
  initialSize = 5
  minIdle = 5
  maxActive = 50
  maxWait = 60000
  timeBetweenEvictionRunsMillis = 60000
  minEvictableIdleTimeMillis = 300000
  validationQuery = "SELECT 'x'"
  testWhileIdle = true
  testOnBorrow = false
  testOnReturn = false
  poolPreparedStatements = true
  maxPoolPreparedStatementPerConnectionSize = 20
  filters = "stat"
}

arr {
  flag = true
}